# Tasks
- [x] Add the "Test All Models" button beside the docs link in the visibility settings modal (`index.html`)
- [x] Create the HTML structure for `#ai-model-tester-modal` dialog overlay (`index.html`)
- [x] Implement the javascript logic for `openAIModelTesterModal()`, `closeAIModelTesterModal()`, and `runModelBatchTest()` (`index.html`)
- [x] Add beautiful responsive cards for rendering live batch test results per model
- [x] Test and push updates to github
