# Task: Fix popovers and dropdowns not closing on outside click reliably

## Tasks
- [X] Change outside click listeners to use `pointerdown` with `capture: true` to bypass `xterm.js` event interception
