1. Project DNA (Permanent): Python-based Windows terminal Gemini CLI with a modular, tool-driven architecture, interactive prompt/TUI, local file/system helpers, and persistent model/API preferences. Its primary goal is practical interactive chat and coding assistance from the terminal.

2. Latest Implementation: Switched the REPL input line to `prompt_toolkit` with persistent file-backed history; kept fixed 1-min cooldown handling; added optional auto-failover; added `/tool`-managed editing tools including fuzzy `apply_patch` (±50 line sliding window & normalized line-ending/whitespace matching), `smart_replace_block` (with trailing whitespace and CRLF fallback matching), and `replace_lines` (direct 1-indexed line range replacement); added a `run_powershell` tool. Notification writes still update `YYYY-MM-DD-HH:MM` in `C:\Users\nahid\notification.txt`.

3. Critical Context: `read_dynamic_prompt` uses `prompt_toolkit`. `apply_patch` now performs fuzzy sliding-window search (±50 lines) and trailing-whitespace normalized comparisons to prevent line drift errors. New tools `replace_lines` and `smart_replace_block` provide low-token alternatives to standard block replacements. `run_powershell` is preferred for `rg`, `Get-Content`, git checks, and tests before editing. Notification writes are best-effort and silent. Persistent state remains in `model_prefs.json`, `prompt_history.txt`, and `api_accounts.lock`.

4. Pending Task: Install `prompt_toolkit`, manually verify prompt editing/cursor behavior, confirm the fixed `01:00` cooldown display, and check notification-file triggers in a real Windows console.
