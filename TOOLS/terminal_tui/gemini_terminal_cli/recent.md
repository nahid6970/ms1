1. Project DNA (Permanent): Python-based Windows terminal Gemini CLI with a modular, tool-driven architecture, interactive prompt/TUI, local file/system helpers, and persistent model/API preferences. Its primary goal is practical interactive chat and coding assistance from the terminal.

2. Latest Implementation: Switched the REPL input line to `prompt_toolkit` for normal cursor movement and history editing; kept the fixed one-minute cooldown handling; added optional auto-failover that retries retryable quota/rate-limit errors across saved API accounts in circular order, skipping accounts already tried in the same turn; surfaced a per-account `Failovers` counter in `/api`; and added an `/tool`-managed `apply_patch` local tool for efficient unified-diff code edits. Notification writes still update `YYYY-MM-DD-HH:MM` in `C:\Users\nahid\notification.txt` on successful completion, quota errors, empty responses, max tool-loop exit, and cooldown expiry.

3. Critical Context: `read_dynamic_prompt` now prefers `prompt_toolkit` and falls back to plain `input()` if the package is missing. `command_history` is session-only and removes consecutive duplicates. `model_cooldowns` stores expiry datetimes, but retry responses always become exactly 60 seconds. Auto-failover only engages on retryable quota/rate-limit errors and tries each saved API account once per turn, incrementing a persistent per-account failover counter when an account is reached. `apply_patch` expects standard unified diffs and rejects mismatched context before writing files. Notification writes are intentionally best-effort and silent. Persistent state remains in `model_prefs.json` and `api_accounts.lock`.

4. Pending Task: Install `prompt_toolkit`, manually verify prompt editing/cursor behavior, confirm the fixed `01:00` cooldown display, and check notification-file triggers in a real Windows console.
