# ~/.config/starship.toml

# Inserts a blank line between commands
add_newline = true

# Customize the prompt format
format = """
[](bold green) [](bold blue) $directory\
$git_branch$git_commit\
$rust$python\
$nodejs\
$line_break\
$character
"""

[character]
# The name of the module we are configuring is 'character'
success_symbol = "[](bold green)"
error_symbol = "[](bold red)"

[directory]
# Here we can customize the path format
format = "[$path]($style)"
style = "bold blue"
# Some common substitutions for directories
[directory.substitutions]
"~" = ""
"Documents" = ""
"Downloads" = ""
"Music" = ""
"Videos" = ""
"Pictures" = ""

[git_branch]
format = "[$symbol$branch]($style)"
style = "bold purple"
symbol = " "

[git_commit]
commit_hash_length = 7
style = "bright-black"
format = "([$hash]($style))"

[git_status]
format = "($all_status$ahead_behind)"
up_to_date = ""
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕${ahead_count}/${behind_count}"
staged = '[+$count](green)'
modified = '[~$count](yellow)'
renamed = '[»$count](blue)'
deleted = '[-$count](red)'
conflicted = '[✕$count](bold red)'
untracked = '[?$count](bold yellow)'
stashed = '[$count](cyan)'

[rust]
symbol = " "
style = "bold red"
format = "[$symbol$version]($style)"

[python]
symbol = " "
style = "bold yellow"
format = "[$symbol$version]($style)"

[nodejs]
symbol = " "
style = "bold green"
format = "[$symbol$version]($style)"

[cmd_duration]
format = "[$duration]($style)"
style = "bold italic white"
min_time = 5000 # Shows command duration if it exceeds 5000ms

[line_break]
disabled = false

# Example of environment variable integration
[env_var.VIRTUAL_ENV]
format = "([$variable(basename .)]($style) )"
style = "bold green"
when = "VIRTUAL_ENV"
